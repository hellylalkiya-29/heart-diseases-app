name: heart_disease_app
description: "A professional Edge-AI Health Diagnostics App"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  tflite_flutter: ^0.12.0 # Updated for compatibility
  shared_preferences: ^2.5.5
  firebase_core: ^3.0.0
  cloud_firestore: ^5.0.0
  cupertino_icons: ^1.0.8
  equatable: ^2.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0